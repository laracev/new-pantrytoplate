<Window x:Class="Pantry_To_Plate.windows.MahlzeitHinzufügenWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:Pantry_To_Plate.windows" mc:Ignorable="d" Title="Mahlzeit hinzufügen" Height="560" Width="920" MinHeight="520" MinWidth="860" WindowStartupLocation="CenterOwner" Background="#FFF7F7FB">

    <Window.Resources>
        <DropShadowEffect x:Key="SoftShadow" BlurRadius="22" ShadowDepth="4" Opacity="0.12"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF232331"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FF5E5E72"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="#FF252535"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFE3E3EC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="14">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#FF2B2B3A"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFE8E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#FFEB477B"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FFD93E72"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FFC93463"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FFCACAD5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="26">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" CornerRadius="26" Background="#FFFFEEF4" Padding="26" Margin="0,0,0,20" Effect="{StaticResource SoftShadow}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Mahlzeit hinzufügen" FontSize="30" FontWeight="Bold" Foreground="#FF202030"/>
                    <TextBlock Text="Suche ein Lebensmittel, gib die Menge ein und füge es deinem Tagesplan hinzu." FontSize="15" Foreground="#FF747486" Margin="0,8,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.15*"/>
                <ColumnDefinition Width="0.95*"/>
                <ColumnDefinition Width="1.05*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="24" Padding="22" Margin="0,0,16,0" Effect="{StaticResource SoftShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Lebensmittel suchen" FontSize="20" FontWeight="Bold"/>

                    <StackPanel Grid.Row="1" Margin="0,18,0,14">
                        <Label Content="Lebensmittel suchen:"/>
                        <TextBox Name="TxtBoxLebensmittelHinzufügen" Height="48" Margin="0,8,0,0" TextWrapping="Wrap" TextChanged="TextBox_TextChanged"/>
                    </StackPanel>

                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Ausgewählte Lebensmittel" FontSize="14" FontWeight="SemiBold" Foreground="#FF6C6C80" Margin="0,0,0,8"/>
                        <ListBox Name="ListBoxLebensmittel" Grid.Row="1" d:ItemsSource="{d:SampleData ItemCount=5}"/>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="24" Padding="22" Margin="0,0,16,0" Effect="{StaticResource SoftShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Menge eingeben" FontSize="20" FontWeight="Bold"/>

                    <StackPanel Grid.Row="1" Margin="0,18,0,18">
                        <Label Content="menge in gramm:"/>
                        <TextBox Name="TxtBoxMenge" Height="54" Margin="0,8,0,0" TextWrapping="Wrap"/>
                    </StackPanel>

                    <Button Grid.Row="2" Name="Btn_LebensMittelHinzufuegen" Content="" Height="56" Click="Btn_LebensMittelHinzufuegen_Click"/>

                    <Label Grid.Row="3" Content="Lebensmittel Hinzufügen" HorizontalContentAlignment="Center" VerticalAlignment="Top" Margin="0,12,0,0"/>
                    <Button Grid.Row="3" Content="Button" Height="54" Margin="0,55,0,0"/>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="24" Padding="22" Effect="{StaticResource SoftShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Treffer auswählen" FontSize="20" FontWeight="Bold"/>
                    <ListBox Name="ListBoxSuchergebnisse" Grid.Row="1" Margin="0,18,0,10" SelectionChanged="ListBoxSuchergebnisse_SelectionChanged"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
